{
  "baseBranch": "origin/main",
  "pipeline": {
    "build": {
      "dependsOn": [
        "^build",
        "$CORS_ORIGIN_URL",
        "$NEXT_PUBLIC_PROD_ORIGIN",
        "$NODE_ENV",
        "$IFRAME_SUPPORT_ENABLED"
      ],
      "outputs": [
        ".next/**",
        "dist/**"
      ]
    },
    "typecheck": {
      "dependsOn": [
        "^typecheck"
      ],
      "outputs": []
    }
  },
  "globalDependencies": [
    "**/.env"
  ]
}